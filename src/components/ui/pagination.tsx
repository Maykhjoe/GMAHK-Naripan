import { ChevronLeft, ChevronRight } from "lucide-react";
export function Pagination({page=1,total=3}:{page?:number;total?:number}){return <nav aria-label="Paginasi" className="flex items-center justify-center gap-2"><button className="grid size-11 place-items-center rounded-full bg-white" aria-label="Halaman sebelumnya"><ChevronLeft className="size-4"/></button>{Array.from({length:total},(_,i)=>i+1).map(v=><button key={v} aria-current={v===page?"page":undefined} className={`grid size-11 place-items-center rounded-full ${v===page?"bg-primary text-white":"bg-white"}`}>{v}</button>)}<button className="grid size-11 place-items-center rounded-full bg-white" aria-label="Halaman berikutnya"><ChevronRight className="size-4"/></button></nav>;}
